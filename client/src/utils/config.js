export const cookieKeyAuth = 'goodie-token';
export const cookieKeyUsername = 'goodie-user';

/* in dev we set our host at 'proxy' property in package.json */
export const jobApiBaseUrl = '/api';

/* in prod set full url because proxy won't work */
// export const jobApiBaseUrl = 'https://abc.com/api';
